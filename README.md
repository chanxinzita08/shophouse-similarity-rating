# Shophouse Facade Similarity Rating — behavioural pretest

Participant-facing jsPsych task: **both facades in a pair are shown side by
side at once**, and the participant rates how similar they are on a 1-7
scale **by clicking a button** (not a keypress) — they can click a different
number to change their answer, then click **Next** to confirm. There is no
confidence rating. This is a separate, simpler sibling to
`eeg_similarity_task/` (which uses sequential presentation + F/J + keyboard
confidence, for EEG time-locking) — this one has no fixation cross, no
sequential timing, no practice trials, no blocks. It exists to collect
**human similarity ratings** on the same pairs already scored by the
automated pipeline (`visual_similarity_score`, `graph_similarity_score`), so
those automated scores can later be normalized against / validated with
human judgement.

It never shows condition labels, cluster numbers, similarity scores, or
filenames to the participant — those are only present in the collected data.

**This version is designed to run away from your computer**: anyone with the
link can open it and their responses are sent automatically to a Google
Sheet as they go (no file to email back). See section 3 for deployment.

## 1. Where the stimulus pool comes from

`trials.csv` is generated by `build_pool.py` from the completed manual
screening pass (`behavioral_pretest/manual_screening_results.csv`), joined
with `behavioral_pretest/candidate_pairs.csv` for the `visual_similarity_score`
/ `graph_similarity_score` columns. Selection rule:

- `screening_score == 100` → **always kept** (253 pairs)
- `screening_score == 80` → **kept in full** (18 pairs — already a small pool)
- `screening_score == 50` → **selectively added**, but only pairs that
  meaningfully widen/densify the visual- and graph-similarity-score
  distribution get pulled in (89 of 187 available, at the current
  `--target-min-per-bin 60` setting) — this is what keeps the final pool from
  being dominated by extreme-similar/extreme-dissimilar pairs only.
- `screening_score == 20` / `Reject` → **always excluded**

Current pool (see `trials_summary.csv` for the live numbers): **360 trials**
— 253 (score 100) + 18 (score 80) + 89 (score 50) — spanning
`visual_similarity_score` 0.8895–1.0 and `graph_similarity_score`
-0.531–1.0.

Images referenced in `trials.csv` are `.jpg`, not the original `.png` —
`build_pool.py` downscales every image to a max of 1000px on the long edge
and re-encodes it as JPEG (quality 82) while copying it into `images/`. The
source photos are lossless PNGs at 2000-3600px / 2-10MB each (2.2GB total for
this pool's 582 images), which is impractical to host on GitHub Pages or to
download in a participant's browser; the downscaled `images/` folder is
~77MB with no visible quality loss at the size the page actually displays
them (max 480px). This only affects the copies in this folder — the
originals in `behavioral_pretest/images/` are untouched.

Regenerate with:

```bash
cd "shophouse genomics"
python3 similarity_rating_pretest/build_pool.py
```

Useful flags: `--target-min-per-bin` (raise to pull in more score-50 pairs,
lower to pull in fewer — 0 disables score-50 additions entirely),
`--n-bins` (how finely the similarity-score range is binned when checking
coverage), `--max-image-occurrence` (default 6), `--seed` (default 42). It
also writes `trials_summary.csv` and syncs `images/` to hold exactly the
files the selected trials need — no other files need to change.

## 2. Running the page locally (for testing before deployment)

`fetch()` of `trials.csv` requires the page to be served over http(s) — it
will not work if you double-click `index.html` (file://).

```bash
cd similarity_rating_pretest
python3 -m http.server 8050
# open http://localhost:8050
```

Everything needed to render the page is vendored locally in `lib/` (jsPsych
core, CSS, plugins — reused from `eeg_similarity_task/lib/`) — there are no
CDN/unpkg.com URLs in `index.html`. Once deployed (section 3), the page
itself needs no server-side code beyond static file hosting; the only thing
that talks to a real backend is the per-trial data submission (section 3.2).

## 3. Deploying so anyone with a link can use it

Two independent pieces: **(3.1)** a Google Sheet that receives responses in
real time, and **(3.2)** static hosting for the page itself (GitHub Pages).
Both are free and only need a Google account and a GitHub account — no
server to maintain.

### 3.1 Server-side data collection (Google Sheets)

1. Go to [sheets.google.com](https://sheets.google.com) and create a new,
   blank spreadsheet (any name, e.g. "Shophouse Similarity Ratings").
2. In the sheet, go to **Extensions → Apps Script**. Delete whatever is in
   the default `Code.gs` editor, and paste in the entire contents of
   [`google_apps_script/Code.gs`](google_apps_script/Code.gs) from this
   folder.
3. Click **Deploy → New deployment**. Next to "Select type", click the gear
   icon and choose **Web app**.
4. Set:
   - **Execute as**: Me (your Google account)
   - **Who has access**: Anyone
5. Click **Deploy**. The first time, Google will ask you to authorize the
   script (click through the "unverified app" warning — it's your own
   script, this is expected for personal Apps Script projects).
6. Copy the **Web app URL** it gives you (ends in `/exec`).
7. Open `experiment.js` in this folder and paste that URL as the value of
   `GOOGLE_SHEET_WEB_APP_URL` near the top of the file, replacing
   `"PASTE_YOUR_DEPLOYED_WEB_APP_URL_HERE"`.
8. Responses will appear as new rows in a sheet tab named `responses`
   (auto-created on the first submission, with a header row) — refresh the
   spreadsheet to see new rows come in live as participants complete trials.

If you ever change `Code.gs`, you need to **Deploy → Manage deployments →
edit (pencil icon) → New version → Deploy** for the change to take effect —
just saving the script file is not enough.

### 3.2 Hosting the page (GitHub Pages)

1. Create a new GitHub repository (e.g. `shophouse-similarity-rating`),
   public, empty (no README/gitignore).
2. Push the entire contents of this `similarity_rating_pretest/` folder
   (with `GOOGLE_SHEET_WEB_APP_URL` already filled in from step 3.1) to that
   repo's `main` branch.
3. In the repo, go to **Settings → Pages**. Under "Build and deployment",
   set **Source: Deploy from a branch**, **Branch: main**, folder **/ (root)**.
   Save.
4. GitHub will give you a public URL, typically
   `https://<username>.github.io/<repo-name>/` — that's the link you send to
   participants.
5. GitHub Pages can take a minute or two to go live after the first push.

### Testing before sending the link to real participants

Open the deployed URL yourself, complete a couple of trials, and confirm new
rows show up in the Google Sheet. Also check the browser console for
`submitToSheet failed` warnings if rows aren't appearing — that usually
means step 3.1.7 (pasting the URL into `experiment.js`) was skipped, or the
deployment's "Who has access" wasn't set to "Anyone".

## 4. Task structure

1. Welcome page
2. Participant information (ID, age, gender, background, familiarity 1-7 —
   same fields as `eeg_similarity_task` for consistency)
3. Instructions — explicitly tells participants: *"Please use the full
   rating scale whenever appropriate, rather than giving the same score
   repeatedly."*
4. Main trials (all 360, one continuous randomized sequence — no blocks, no
   practice): both facades shown side by side. A **"Trial X of 360"**
   counter is shown above every pair. Click a number **1–7** (1 = not
   similar at all, 4 = moderate/unsure, 7 = highly similar) — clicking a
   different number changes the selection — then click **Next** to confirm
   and advance. No score is disallowed; participants are free to use the
   extremes as often as they judge appropriate. A **Back** button (disabled
   only on trial 1) lets the participant return to any earlier trial one
   step at a time; that trial's previous rating is shown pre-selected, and
   clicking a different number + **Next** overwrites it and moves forward
   again (see section 5 for exactly what "overwrites" means server-side).
5. End page — responses have already been sent to the Google Sheet
   automatically as the participant went; there's also a **Download CSV**
   button for a personal/local copy.

All 360 trials are handled by a single custom screen (`ratingAppNode` /
`renderCurrentTrial()` in `experiment.js`) that manually tracks a
`currentIndex` into a pre-built `trialRecords` array, rather than one
jsPsych timeline node per trial — jsPsych's timeline only moves forward, so
Back/re-answer needed manual state management instead.

Left/right position of `image_A` vs `image_B` is randomized per trial
(`left_right_swapped`), independent of the underlying pair or condition, so
screen side is never confounded with anything.

## 5. How data collection actually works, and its limits

Right after each trial is confirmed (clicking **Next** — including
re-confirming a trial you revisited via **Back**), the result is:
1. Updated in place in the in-page `trialRecords[]` array (indexed by
   `trial_index_global`, so a revised answer overwrites the in-memory copy
   of that trial),
2. The **entire current `trialRecords[]` array** is re-written to
   `localStorage` (key: `similarity_rating_progress_<participant_id>`) —
   this is autosaved on literally every "Next" click, i.e. every single
   trial, which is the finest granularity possible (there's no reason to
   save less often than this — the write is local and effectively free).
3. **POSTed to the Google Sheets Web App** (`submitToSheet()` in
   `experiment.js`) — this is the real, centralized data collection.
   `Code.gs` writes it to a **per-participant tab**, named after
   `participant_id` (e.g. a participant who enters `P01` gets their own tab
   called `P01`, auto-created on their first submitted trial). This layout
   is intentional for this study's expected scale (well under 30
   participants) — Google Sheets caps a spreadsheet at roughly 200 tabs, so
   **do not reuse this per-participant-tab design for a large-N online
   study**; switch back to one shared tab (see git history / ask to revert)
   if participant count could approach that limit.

**This is fire-and-forget, not blocking.** `submitToSheet()`'s `fetch(...)`
call is never `await`-ed, and the "Next" button's click handler calls
`finalizeRecord()` then immediately advances `currentIndex` / re-renders the
next trial in the same synchronous call — it does not wait for the Google
Sheets request to resolve (or fail). So a slow or flaky connection cannot
stall or block the participant from moving to the next trial; worst case, a
given row just arrives at the Sheet a bit late (or, if `fetch` truly fails,
not at all — but it's always safe in `localStorage`, and in the downloaded
CSV, regardless).

**Important: revisiting and changing an answer sends a *new* row rather
than editing the old one** — Apps Script's `appendRow` only appends, it
doesn't look up and update existing rows. So if a participant uses Back to
change trial 42 from a 3 to a 6, their tab will end up with *two* rows for
`trial_index_global = 42`, one with `similarity_rating = 3` and an earlier
`server_received_at`, one with `6` and a later one. The downloaded CSV
(`exportResults()` / Download CSV button) does NOT have this issue — it
always reflects the current, final in-memory state of `trialRecords[]`, one
row per trial.

**Concurrent submissions are serialized with `LockService`**: if two
participants submit at close to the same instant, `doPost` in `Code.gs`
acquires a script-wide lock before touching any sheet/tab, so one request
always fully finishes writing (including creating a new tab, if that
participant's first trial) before the next one starts — this avoids a race
where two simultaneous "does this tab exist yet?" checks could both say no
and try to create it at once.

Sending per-trial (rather than one bulk upload at the end) means a
participant who closes the tab partway through still leaves their completed
trials in the Sheet. The `fetch(...)` call uses `mode: "no-cors"` because
Apps Script Web Apps don't return CORS headers — this is expected and means
the page can't confirm the submission succeeded from JS, but the row still
gets written. If `GOOGLE_SHEET_WEB_APP_URL` is left as the placeholder, the
page still works end-to-end (rating/Next/Back/etc.), it just logs a console
warning and doesn't send anywhere except `localStorage` and the optional
CSV download — useful for local testing without a deployed Sheet.

There's no participant authentication or duplicate-submission protection
beyond `participant_id` being whatever the participant typed in — if you
need to guard against the same person submitting twice, dedupe on
`participant_id` when analyzing the Sheet.

### Completion tracking (the `completions` tab)

Once a participant reaches the end page — which only happens after every
one of the 360 trials has a rating, since "Next" is disabled until one is
selected — `submitCompletion()` fires once (same fire-and-forget pattern as
above) with:

```
participant_id, completed=true, completion_time, total_answered
```

`Code.gs` detects this payload (`type: "completion"`) and routes it to a
**single shared `completions` sheet tab** (unlike the per-participant
`responses` tabs — a completion roster is small and easiest to keep in one
place), auto-created on first use. Use this tab to tell who actually
finished vs. who dropped out partway through — anyone who closed the tab
early will have rows in their own per-participant tab but no row in
`completions`.

**If you already deployed an earlier version of `Code.gs`** (before this
`completions` tab / per-participant-tab layout existed), you need to
re-paste the updated `google_apps_script/Code.gs` into your Apps Script
project and create a **new deployment version** (Deploy → Manage
deployments → edit → New version → Deploy) for this to take effect — just
saving the script isn't enough, same as any other `Code.gs` change.

### Cleaning rules before analysis

When you pull data out of the Sheet for analysis, apply both of these:

1. **Dedupe each participant's tab**: group by `trial_index_global` (within
   that one participant's tab), keep only the row with the latest
   `server_received_at` (falls back to `timestamp` if needed) — this is the
   participant's final answer for that trial after any Back-revisions.
2. **Filter to completed participants only**: keep only `participant_id`
   values that have a row in the `completions` tab with `completed=true`.
   Anyone who dropped out mid-task will have partial `responses` rows but no
   `completions` row — exclude them from the formal pretest results.

## 6. Data field reference

Same fields land in the Google Sheet (`Code.gs`'s `COLUMNS`, plus a
server-side `server_received_at`) and in the local CSV download
(`TRIAL_CSV_COLUMNS` in `experiment.js`) — keep the two lists in sync if you
add/remove fields.

```
participant_id            entered on the participant-info page
trial_index_global        0-based index across the whole randomized sequence
trial_id                  from trials.csv (traceable back to candidate_pairs.csv)
condition                 VpSp / VpSn / VnSp / VnSn (not shown to participant)
original_image_A/B        trials.csv image_A / image_B, regardless of display side
left_image, right_image   whichever image was actually shown on that side
left_right_swapped        true if image_B was shown on the left
visual_A, visual_B        visual cluster labels (not shown to participant)
graph_A, graph_B          graph/syntax cluster labels (not shown to participant)
visual_similarity_score   continuous cosine similarity (visual embedding space)
graph_similarity_score    continuous cosine similarity (graph/PCA embedding space)
screening_score           100 / 80 / 50 — manual screening quality tier for this pair
similarity_rating         1-7, the participant's response (this is the key DV)
rating_onset              performance.now() ms, pair+question onset
rating_rt_ms              ms from rating_onset to clicking Next
trial_end_time            performance.now() ms, end of trial
timestamp                 wall-clock ISO string, written at trial end
server_received_at        (Google Sheet only) wall-clock time Apps Script received the row
```

## 7. Purpose / analysis intent

This task exists to collect **human similarity_rating** values on the same
pairs already carrying `visual_similarity_score` and `graph_similarity_score`
from the automated pipeline. The intended analysis is to correlate/normalize
human ratings against those two automated scores (e.g. does human-perceived
similarity track visual embedding distance, graph/syntax distance, both, or
neither) — this is a calibration/validation step, not the EEG task itself.

## 8. File layout

```
similarity_rating_pretest/
  index.html               loads lib/ (jsPsych) + style.css + experiment.js
  experiment.js            trial-building / submitToSheet / CSV export logic (GOOGLE_SHEET_WEB_APP_URL + CONFIG at top)
  style.css                participant-facing styling only
  lib/                     vendored jsPsych core + plugins + CSS (reused from eeg_similarity_task/lib/)
  google_apps_script/
    Code.gs                paste into Apps Script when deploying (see section 3.1)
  trials.csv               stimulus pool, generated by build_pool.py (see section 1)
  trials_summary.csv       pool composition + similarity-score coverage stats
  build_pool.py            regenerates trials.csv (+ syncs images/) from manual_screening_results.csv
  images/                  synced automatically by build_pool.py
  README.md                this file
```
